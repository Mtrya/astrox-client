[project]
name = "astrox-client"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "requests>=2.32.5",
]

[dependency-groups]
dev = [
    "datamodel-code-generator[http]>=0.53.0",
    "pytest>=9.0.2",
]
